---
"@truefoundry/trueforge-core": patch
"@truefoundry/trueforge": patch
---

Add `opencode-go` model provider: OpenCode Zen Go gateway presets in the model catalog, an OpenAI-compatible adapter case, and per-session `x-opencode-session` / `user-agent` headers on model calls (required by OpenCode Go).
